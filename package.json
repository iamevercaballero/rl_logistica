{
  "name": "rl-logistica",
  "private": true,
  "description": "Monorepo RL Logística WMS — backend (NestJS) y frontend (React/Vite). Cada app tiene su propio package.json.",
  "engines": {
    "node": ">=20"
  }
}
